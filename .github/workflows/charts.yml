name: Rebuild charts

on:
  schedule:
    - cron: "0 21 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  charts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install R packages
        run: install.packages(c("ggplot2", "jsonlite"))
        shell: Rscript {0}

      - name: Append today's prices
        env:
          FINNHUB_KEY: ${{ secrets.FINNHUB_KEY }}
        run: python3 update_prices.py

      - name: Rebuild the charts
        env:
          FINNHUB_KEY: ${{ secrets.FINNHUB_KEY }}
        run: Rscript charts.R

      - name: Commit if anything changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add contribution.png allocation.png data/
          git diff --staged --quiet || git commit -m "Update prices and charts $(date -u +%Y-%m-%d)"
          git push
